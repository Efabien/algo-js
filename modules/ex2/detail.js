module.exports = (arg, { logger }) => {
  logger.info(`
    Your function takes a string as argument. Let call it input.
    Your function should return true if input is a palendrom, false otherwise.
  `)
};